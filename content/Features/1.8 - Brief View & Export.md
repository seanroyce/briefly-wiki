---
title: "1.8 — Brief View & Export"
phase: 1
section: "1.8"
status: complete
completion: 100
category: export
priority: p0
spec_ref: "§2.6, §7.5"
tags:
  - feature
---

# 1.8 — Brief View & Export

Read-only brief view at `/brief/[id]` — brief header (name, status badge, last modified), section content blocks, export dropdown. PDF export (`jspdf`, `GET /api/brief/[id]/export/pdf`). Markdown export (`GET /api/brief/[id]/export/markdown`). Brief duplication from view header.

## Remaining

- [x] `/brief/:id/edit` — `WizardClient` with `editMode` prop; skips Step 1 processing gate, all steps directly navigable

## See Also

- [[Phases/Phase 1 - MVP]]
