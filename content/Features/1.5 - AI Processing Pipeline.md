---
title: "1.5 — AI Processing Pipeline"
phase: 1
section: "1.5"
status: in-progress
completion: 90
category: ai
priority: p0
spec_ref: "§2.3, §13.1"
tags:
  - feature
---

# 1.5 — AI Processing Pipeline

Anthropic Claude API integration. `lib/ai/client.ts`, `lib/ai/prompts.ts` (system prompt + section tool schema), `lib/ai/retry.ts`. `processContext` server action (per-section structured output via tool use). `processAllNotes` (fan-out across all 9 sections in parallel). `analyzeGaps` + `lib/ai/gap-prompts.ts`.

## Remaining

- [x] `POST /api/ai/refine` — section refinement endpoint, wired to "Ask AI to help" in `BriefSectionCard`
- [ ] `lib/ai/parse.ts` — typed response parser / error normalizer
- [ ] Rate limiting (10 process/hr, 20 gaps/hr, 30 refine/hr per user — Upstash Redis)
- [ ] `ai_metadata` tracking on brief row (model, tokens, processing time)

## See Also

- [[Phases/Phase 1 - MVP]]
- [[AI Pipeline]]
- [[Extensibility]]
